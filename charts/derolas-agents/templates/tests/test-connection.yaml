apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "derolas-agents.fullname" . }}-test-connection"
  labels:
    {{- include "derolas-agents.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "derolas-agents.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
